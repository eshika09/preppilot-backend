package com.example.preppilot.demo.entity;

public enum AnalysisMode {
    OA_PAPER,        // user uploads OA PDF → topics, difficulty, revision
    JOB_DESCRIPTION, // user uploads JD PDF → skills to highlight, resume tips
    PREP_PLAN        // status-based → AI reads community experiences → prep plan
}
